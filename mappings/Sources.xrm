logical-source People {
	type json
	source "stdin" // this is usually a file, but we want to pass this through stdin
	iterator "$.characters[*]"

	referenceables
		id
		name
		firstname
		lastname
		hair
}

logical-source PeopleXML {
	type json
	source "stdin" // this is usually a file, but we want to pass this through stdin
	iterator "$.characters[*]"

	referenceables
		id
		name
		firstname
		lastname
		hair
}