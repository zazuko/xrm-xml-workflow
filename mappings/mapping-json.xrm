output carml

map person1 from PeopleJsonExample { 
	subject template personIri with id;

	types
		schema.Person

	properties
		schema.givenName from firstname;
		schema.lastName from lastname;
		ex.hairColor from hair;
}